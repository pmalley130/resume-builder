Use an LLM to parse bullet points from personal resumes, parse job descriptions, then create a resume from that data.

Current status: Successfully creates new bullet points aligning with job description
To-do: Tie those bullet points back to the roles they're from and generate a PDF